[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "optionsahoy-langchain"
version = "0.1.0"
description = "LangChain tools for the OptionsAhoy equity-compensation calculators."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "AlphaLatitude Inc." }]
keywords = ["langchain", "tools", "equity-compensation", "iso", "amt", "nso", "rsu", "qsbs"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Intended Audience :: Developers",
    "Topic :: Office/Business :: Financial",
]
dependencies = [
    "optionsahoy>=0.1.0",
    "langchain-core>=0.3",
    "pydantic>=2",
]

[project.urls]
Homepage = "https://optionsahoy.com"
Repository = "https://github.com/AlvisoOculus/optionsahoy-mcp"
Documentation = "https://optionsahoy.com/for-agents"

[project.optional-dependencies]
dev = ["pytest>=8", "ruff>=0.5"]

[tool.hatch.build.targets.wheel]
packages = ["langchain_optionsahoy"]

[tool.ruff]
line-length = 100

# Published metadata depends on `optionsahoy` by version (see [project] dependencies).
# For local development, install the sibling package editable first so the version
# dependency resolves to your working tree instead of PyPI:
#   pip install -e integrations/python/optionsahoy
#   pip install -e integrations/python/optionsahoy-langchain
